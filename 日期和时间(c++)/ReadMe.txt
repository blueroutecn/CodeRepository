时间和日期（c++）：

KeyWorlds: time(), localtime(), snprintf(), strftime(), ctime(), asctime(), 

/* ------ date_time.h ------ */
1. 跨平台、windows、unixs、linux平台下的日期获取。

2. 跨平台、windows、unixs、linux平台下的日期和时间戳的转换。