原生-文件读取与字符串解析(c++):

KeyWords: stringstream, getline(), strtok(), s.substr(), s.find_first_of()

/* ------ fileReader&stringParse.h ------*/
1. 文件读取流 ifstream与按行读取getline（is， str， delim）。

2. 字符串分割解析strtok，getline, s.find_first_of()+s.substr(front, len)。

3. 字符串string转double，atof（）。